#!/bin/sh

if [ -f "/Volumes/secure/etc/netrc" ]; then
	if [ ! -h "$HOME/.netrc" ] || [ `readlink $HOME/.netrc` != "/Volumes/secure/etc/netrc" ]; then
		ln -s /Volumes/secure/etc/netrc $HOME/.netrc
	fi
fi

